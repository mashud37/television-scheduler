[tool.ruff]
line-length = 110

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP"]

[tool.ruff.lint.per-file-ignores]
# app.py embeds the HTML/CSS page templates as string literals; rewrapping them
# would hurt readability without making the markup any clearer.
"tvsched/app.py" = ["E501"]
